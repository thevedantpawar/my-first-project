# Microns control plane — dependencies.
# Pinned for reproducible Docker builds. Bump deliberately, not accidentally.

# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.52.3
python-multipart==0.0.32
itsdangerous==2.2.0

# --- Data / persistence ---
SQLAlchemy==2.0.52
alembic==1.16.5
psycopg[binary]==3.3.4
pydantic==2.13.4
pydantic-settings==2.15.0
email-validator==2.4.1

# --- Security ---
# argon2 for passwords, Fernet (cryptography) for the tenant secret escrow.
argon2-cffi==25.1.0
cryptography==50.0.0

# --- External services ---
httpx==0.28.1
stripe==14.1.0
tenacity==9.1.4

# --- Test tooling ---
pytest==9.1.1
